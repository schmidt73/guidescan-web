(ns guidescan-web.query.render
  "This namespace exposes the core facilities to render the results of
  a gRNA query job in various formats.")

;; (defmulti render-query-result
;;   (fn [format query-result] format))
  
;; (defmethod render-query-result)
